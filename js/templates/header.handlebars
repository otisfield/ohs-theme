<nav class="navbar navbar-inverse">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{homeLink}}"><img src="{{logo}}"></a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                {{#each navigation}}
                <li><a href="{{url}}" data-id="{{ID}}" target="{{target}}" title="{{title}}"><span>{{title}}</span></a></li>
                {{/each}}
                <li class="social-media social-media-header" data-template="socialMedia" data-source="/wp-json/ohs/v1/social"></li>
            </ul>
        </div>
    </div>
</nav>