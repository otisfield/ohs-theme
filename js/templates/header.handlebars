<nav class="navbar navbar-inverse">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">OHS</a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                {{#each this}}
                <li><a href="{{url}}" data-id="{{ID}}" target="{{target}}" title="{{title}}"><span>{{title}}</span></a></li>
                {{/each}}
                <li class="social-media social-media-header" data-template="socialMedia" data-source="data/social.json"></li>
            </ul>
        </div>
    </div>
</nav>